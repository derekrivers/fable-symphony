{
  "name": "fable-symphony",
  "version": "0.1.0",
  "description": "Symphony: orchestrates Claude Code agents against a Linear-backed issue queue (Claude edition of the Symphony spec)",
  "type": "module",
  "license": "Apache-2.0",
  "bin": {
    "symphony": "./dist/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "tsx src/cli.ts"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "liquidjs": "^10.16.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
